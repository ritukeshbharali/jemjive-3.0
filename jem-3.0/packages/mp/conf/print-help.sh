
cat <<EOF
  --with-mpi            : use the Message Passing Interface.
  --without-mpi         : do not use MPI.
                         (default: --with-mpi)

  --mpi-libs=LIBS       : use LIBS to link with MPI.
  --mpi-libdirs=DIRS    : search in DIRS for the MPI libraries.
  --mpi-incdirs=DIRS    : search in DIRS for the MPI header files.
EOF
